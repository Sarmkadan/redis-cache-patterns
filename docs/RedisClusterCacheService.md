# RedisClusterCacheService

A distributed, cluster-aware caching service built on top of Redis that provides high availability, automatic failover, and consistent behavior across a Redis Cluster. It supports typed value serialization, sliding and absolute expiration policies, distributed locking, key pattern operations, and real-time cache statistics.

## API

### Constructor
